<script lang="ts">
	// Just loads the test presentation. This pattern is deliberate and is so that presentation components aren't tied to the outer page structure. They can be re-used elsewhere etc.
	import ExamplePresentation from "./ExamplePresentation.svelte";
</script>

<ExamplePresentation />
